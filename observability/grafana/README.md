# Grafana

Grafana is provisioned automatically by Docker Compose. Open `http://localhost:3001` and use `admin/admin` for the development environment. The Prometheus datasource and SRE Overview dashboard are provisioned from `provisioning/` and `dashboards/`.
