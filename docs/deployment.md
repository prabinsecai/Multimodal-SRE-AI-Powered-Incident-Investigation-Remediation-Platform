# Deployment
Use Docker Compose locally. For production, build/push the backend and frontend images and deploy them to ECS/EKS. Add managed PostgreSQL/Redis/Qdrant, TLS, secret management, network policies and real Kubernetes provider adapters.
